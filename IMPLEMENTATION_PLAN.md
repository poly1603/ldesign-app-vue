# 🚀 app-vue 核心功能实施计划

> 专注于应用基础设施，打造功能强大、支持多种应用形态的框架

---

## 📊 当前状态分析

### 已集成的基础能力（14个）

| 能力 | 包 | 状态 |
|------|------|------|
| 路由系统 | `@ldesign/router-vue` | ✅ |
| 状态管理 | `@ldesign/store-vue` | ✅ |
| 国际化 | `@ldesign/i18n-vue` | ✅ |
| 主题/暗黑模式 | `@ldesign/color-vue` | ✅ |
| 尺寸系统 | `@ldesign/size-vue` | ✅ |
| HTTP 请求 | `@ldesign/http-vue` | ✅ |
| 缓存 | `@ldesign/cache` | ✅ |
| 日志 | `@ldesign/logger-vue` | ✅ |
| 错误处理 | `@ldesign/error-vue` | ✅ |
| 用户追踪 | `@ldesign/tracker-vue` | ✅ |
| 通知系统 | `@ldesign/notification-vue` | ✅ |
| 加密 | `@ldesign/crypto-vue` | ✅ |
| 设备检测 | `@ldesign/device-vue` | ✅ |
| 模板系统 | `@ldesign/template-vue` | ✅ |

### 缺失的核心能力

| 能力 | 重要性 | 状态 |
|------|--------|------|
| **认证系统** | 🔴 必需 | ❌ 缺失 |
| **权限系统** | 🔴 必需 | ⚠️ 有基础，未集成 |
| **布局系统** | 🔴 必需 | ❌ 缺失 |
| **菜单系统** | 🔴 必需 | ❌ 缺失 |
| **标签页系统** | 🟡 重要 | ❌ 缺失 |
| **面包屑** | 🟡 重要 | ❌ 缺失 |
| **实时通信** | 🟡 重要 | ⚠️ 有 composable，未集成 |
| **命令面板** | 🟢 增强 | ❌ 缺失 |
| **偏好持久化** | 🟢 增强 | ❌ 缺失 |
| **离线/PWA** | 🟢 增强 | ❌ 缺失 |
| **微前端** | 🟢 增强 | ❌ 缺失 |

---

## 🎯 核心功能实施计划

### 第一阶段：认证与权限（支撑所有应用形态）

#### 1.1 认证系统 (Auth) ⭐⭐⭐⭐⭐

**状态**: ❌ 需要新建 `packages/auth`

**核心功能**:
- Token 管理（JWT、刷新 Token）
- 登录/登出/注册流程
- OAuth2/SSO 支持
- 自动刷新、过期处理
- 记住我、自动登录

**包结构**:
```
packages/auth/
├── packages/
│   ├── core/
│   │   └── src/
│   │       ├── types.ts              # 类型定义
│   │       ├── auth-manager.ts       # 认证管理器
│   │       ├── token-manager.ts      # Token 管理
│   │       ├── session-manager.ts    # 会话管理
│   │       ├── strategies/
│   │       │   ├── jwt.ts            # JWT 策略
│   │       │   ├── oauth2.ts         # OAuth2 策略
│   │       │   └── session.ts        # Session 策略
│   │       └── index.ts
│   └── vue/
│       └── src/
│           ├── composables/
│           │   ├── useAuth.ts        # 认证状态
│           │   ├── useUser.ts        # 用户信息
│           │   └── useLogin.ts       # 登录逻辑
│           ├── components/
│           │   ├── AuthGuard.vue     # 认证守卫组件
│           │   └── LoginForm.vue     # 登录表单
│           ├── plugin/
│           │   └── index.ts          # Vue 插件
│           └── index.ts
```

**工作量**: 20-25 小时

---

#### 1.2 权限系统 (Permission) ⭐⭐⭐⭐⭐

**状态**: ⚠️ `@ldesign/router-core` 已有基础实现，需要增强并集成

**核心功能**:
- 路由级权限控制
- 按钮/操作级权限
- 角色管理 (RBAC)
- 动态权限加载
- 权限指令 (v-permission)

**现有代码**: `packages/router/packages/core/src/features/permissions.ts`

**需要做的**:
```
packages/permission/  （或增强 router-core）
├── packages/
│   ├── core/
│   │   └── src/
│   │       ├── types.ts
│   │       ├── permission-manager.ts  # 权限管理器
│   │       ├── role-manager.ts        # 角色管理
│   │       ├── cache.ts               # 权限缓存
│   │       └── index.ts
│   └── vue/
│       └── src/
│           ├── composables/
│           │   ├── usePermission.ts   # 权限检查
│           │   └── useRole.ts         # 角色检查
│           ├── directives/
│           │   └── permission.ts      # v-permission 指令
│           ├── components/
│           │   └── PermissionGuard.vue
│           ├── plugin/
│           └── index.ts
```

**工作量**: 15-20 小时

---

### 第二阶段：布局与导航（定义应用形态）

#### 2.1 布局系统 (Layout) ⭐⭐⭐⭐⭐

**状态**: ❌ 需要新建 `packages/layout`

**核心功能**:
- 多种布局模式：
  - `admin` - 后台管理（侧边栏+顶栏）
  - `portal` - 门户网站（顶部导航）
  - `dashboard` - 仪表盘（全屏+面板）
  - `mobile` - 移动端（底部导航）
  - `blank` - 空白布局
- 布局切换
- 响应式适配
- 侧边栏折叠

**包结构**:
```
packages/layout/
├── packages/
│   ├── core/
│   │   └── src/
│   │       ├── types.ts
│   │       ├── layout-manager.ts
│   │       ├── responsive.ts         # 响应式逻辑
│   │       └── index.ts
│   └── vue/
│       └── src/
│           ├── layouts/
│           │   ├── AdminLayout.vue   # 后台管理布局
│           │   ├── PortalLayout.vue  # 门户布局
│           │   ├── DashboardLayout.vue
│           │   ├── MobileLayout.vue
│           │   └── BlankLayout.vue
│           ├── components/
│           │   ├── LayoutHeader.vue
│           │   ├── LayoutSider.vue
│           │   ├── LayoutContent.vue
│           │   ├── LayoutFooter.vue
│           │   └── LayoutTabs.vue
│           ├── composables/
│           │   ├── useLayout.ts
│           │   └── useSider.ts
│           ├── plugin/
│           └── index.ts
```

**工作量**: 25-30 小时

---

#### 2.2 菜单系统 (Menu) ⭐⭐⭐⭐

**状态**: ❌ 需要新建 `packages/menu`

**参考**: `libraries/webcomponent/src/components/menu` 有完整的 Web Component 实现

**核心功能**:
- 数据驱动的菜单
- 权限过滤（与权限系统联动）
- 图标支持
- 多级菜单
- 菜单折叠
- 横向/纵向模式

**包结构**:
```
packages/menu/
├── packages/
│   ├── core/
│   │   └── src/
│   │       ├── types.ts
│   │       ├── menu-manager.ts
│   │       ├── menu-filter.ts        # 权限过滤
│   │       ├── menu-utils.ts
│   │       └── index.ts
│   └── vue/
│       └── src/
│           ├── components/
│           │   ├── Menu.vue
│           │   ├── MenuItem.vue
│           │   ├── SubMenu.vue
│           │   └── MenuGroup.vue
│           ├── composables/
│           │   ├── useMenu.ts
│           │   └── useMenuState.ts
│           ├── plugin/
│           └── index.ts
```

**工作量**: 20-25 小时

---

#### 2.3 标签页系统 (Tabs) ⭐⭐⭐⭐

**状态**: ❌ 需要新建 `packages/tabs`

**参考**: `libraries/webcomponent/src/components/tabs` 有完整实现

**核心功能**:
- 多标签页管理
- 标签页缓存（keep-alive）
- 右键菜单（关闭其他、关闭右侧等）
- 标签页拖拽排序
- 固定标签页
- 与路由联动

**包结构**:
```
packages/tabs/
├── packages/
│   ├── core/
│   │   └── src/
│   │       ├── types.ts
│   │       ├── tab-manager.ts
│   │       ├── tab-cache.ts          # 缓存管理
│   │       ├── tab-history.ts        # 历史记录
│   │       └── index.ts
│   └── vue/
│       └── src/
│           ├── components/
│           │   ├── TabBar.vue
│           │   ├── TabPane.vue
│           │   └── TabContextMenu.vue
│           ├── composables/
│           │   ├── useTabs.ts
│           │   └── useTabCache.ts
│           ├── plugin/
│           └── index.ts
```

**工作量**: 20-25 小时

---

#### 2.4 面包屑 (Breadcrumb) ⭐⭐⭐

**状态**: ❌ 需要新建（可集成到 router 或 layout）

**核心功能**:
- 基于路由自动生成
- 自定义面包屑
- 图标支持
- 下拉菜单（子页面快速跳转）

**建议**: 作为 `@ldesign/layout-vue` 的一部分

**工作量**: 5-8 小时

---

### 第三阶段：实时通信与状态同步

#### 3.1 WebSocket/实时通信 ⭐⭐⭐

**状态**: ⚠️ `@ldesign/http-vue` 已有 `useWebSocket`、`useSSE`

**需要做的**:
- [ ] 创建全局 WebSocket 管理器
- [ ] 实现消息订阅/发布
- [ ] 支持断线重连
- [ ] 集成到 app-vue

**工作量**: 8-10 小时（集成+增强）

---

#### 3.2 状态持久化 ⭐⭐⭐

**状态**: ⚠️ 有 `@ldesign/cache`，但缺少与 store 的深度集成

**核心功能**:
- 用户偏好自动持久化（主题、语言、布局）
- 状态恢复
- 多标签页状态同步
- 跨浏览器同步（可选）

**需要做的**:
- [ ] 创建 `usePersistedState` composable
- [ ] 与 store 集成
- [ ] 支持选择性持久化

**工作量**: 10-12 小时

---

### 第四阶段：增强功能

#### 4.1 命令面板 (Command Palette) ⭐⭐⭐

**状态**: ❌ 需要新建

**核心功能**:
- 全局快捷键唤起（Ctrl+K / Cmd+K）
- 页面快速跳转
- 操作搜索
- 最近访问
- 自定义命令

**包结构**:
```
packages/command/
├── packages/
│   ├── core/
│   │   └── src/
│   │       ├── types.ts
│   │       ├── command-manager.ts
│   │       ├── search.ts
│   │       └── index.ts
│   └── vue/
│       └── src/
│           ├── components/
│           │   └── CommandPalette.vue
│           ├── composables/
│           │   └── useCommand.ts
│           └── index.ts
```

**工作量**: 15-18 小时

---

#### 4.2 离线支持 / PWA ⭐⭐

**状态**: ❌ 需要配置

**核心功能**:
- Service Worker 注册
- 资源缓存策略
- 离线页面
- 更新提示

**需要做的**:
- [ ] 添加 vite-plugin-pwa
- [ ] 配置缓存策略
- [ ] 创建离线页面

**工作量**: 8-10 小时

---

#### 4.3 微前端支持 ⭐⭐

**状态**: ❌ 需要新建

**核心功能**:
- 子应用加载
- 应用间通信
- 样式隔离
- 生命周期管理

**建议**: 使用 qiankun 或 micro-app

**工作量**: 20-30 小时

---

## 📊 优先级矩阵

```
                    重要性
                      ↑
         高 ┌─────────┬─────────┐
            │ 认证    │ 布局    │
            │ 权限    │ 菜单    │
            │ 标签页  │         │
            ├─────────┼─────────┤
         中 │ 实时通信│ 面包屑  │
            │ 状态持久│ 命令面板│
            ├─────────┼─────────┤
         低 │ PWA     │ 微前端  │
            │         │         │
            └─────────┴─────────┘
              必需      增强    →  紧急性
```

---

## � 实施路线图

### 第一阶段（Week 1-2）：认证与权限

```
Week 1:
├── Day 1-2: 创建 packages/auth/packages/core
├── Day 3-4: 创建 packages/auth/packages/vue
└── Day 5:   集成到 app-vue

Week 2:
├── Day 1-2: 增强 packages/permission（基于 router-core）
├── Day 3:   创建 v-permission 指令
├── Day 4-5: 集成权限到 app-vue
```

**成果**: 完整的认证与权限系统

---

### 第二阶段（Week 3-4）：布局与菜单

```
Week 3:
├── Day 1-3: 创建 packages/layout/packages/core
├── Day 4-5: 创建 packages/layout/packages/vue

Week 4:
├── Day 1-3: 创建 packages/menu
├── Day 4-5: 布局与菜单集成
```

**成果**: 支持多种布局模式的应用框架

---

### 第三阶段（Week 5-6）：标签页与面包屑

```
Week 5:
├── Day 1-3: 创建 packages/tabs
├── Day 4-5: 标签页缓存与历史

Week 6:
├── Day 1-2: 面包屑集成
├── Day 3-5: 整体调试与优化
```

**成果**: 完整的后台管理框架

---

### 第四阶段（Week 7-8）：增强功能

```
Week 7:
├── Day 1-3: WebSocket 全局管理
├── Day 4-5: 状态持久化

Week 8:
├── Day 1-3: 命令面板
├── Day 4-5: PWA 支持
```

**成果**: 功能完善的企业级应用框架

---

## 📁 完成后的应用架构

```
apps/app-vue/src/
├── api/                      # API 层
│   ├── auth.ts               # 认证 API
│   ├── user.ts               # 用户 API
│   └── index.ts
├── layouts/                  # 布局组件
│   ├── AdminLayout.vue       # 后台管理布局
│   ├── PortalLayout.vue      # 门户布局
│   └── BlankLayout.vue       # 空白布局
├── locales/                  # 国际化
├── plugins/                  # 引擎插件
│   ├── index.ts
│   ├── auth.ts               # 🆕 认证插件
│   ├── permission.ts         # 🆕 权限插件
│   ├── layout.ts             # 🆕 布局插件
│   ├── menu.ts               # 🆕 菜单插件
│   ├── tabs.ts               # 🆕 标签页插件
│   └── ...existing...
├── router/
│   ├── index.ts
│   ├── guards.ts             # 🆕 路由守卫
│   └── routes/               # 🆕 模块化路由
│       ├── auth.ts
│       ├── dashboard.ts
│       └── settings.ts
├── stores/                   # 状态仓库
│   ├── auth.ts               # 🆕 认证状态
│   ├── user.ts               # 🆕 用户状态
│   ├── permission.ts         # 🆕 权限状态
│   ├── layout.ts             # 🆕 布局状态
│   ├── tabs.ts               # 🆕 标签页状态
│   └── app.ts                # 🆕 应用状态
├── views/
│   ├── auth/                 # 认证相关
│   │   ├── Login.vue
│   │   ├── Register.vue
│   │   └── ForgotPassword.vue
│   ├── error/                # 错误页面
│   │   ├── 403.vue
│   │   ├── 404.vue
│   │   └── 500.vue
│   ├── dashboard/            # 仪表盘
│   │   └── Index.vue
│   └── ...other views...
├── App.vue
└── main.ts
```

---

## 🎯 支持的应用形态

完成所有功能后，可以快速构建以下类型的应用：

### 1. 后台管理系统 (Admin)
- ✅ 侧边栏 + 顶栏布局
- ✅ 多标签页
- ✅ 权限控制
- ✅ 菜单管理

### 2. 门户网站 (Portal)
- ✅ 顶部导航布局
- ✅ 用户认证
- ✅ 响应式设计

### 3. 仪表盘 (Dashboard)
- ✅ 全屏布局
- ✅ 实时数据推送
- ✅ 可拖拽面板

### 4. 移动端应用 (Mobile)
- ✅ 底部导航布局
- ✅ 手势支持
- ✅ PWA 离线

### 5. 多租户 SaaS
- ✅ 动态权限
- ✅ 租户隔离
- ✅ 主题定制

---

## 📦 需要新建的 packages 汇总

| 包名 | 路径 | 优先级 | 工作量 |
|------|------|--------|--------|
| `@ldesign/auth-core` | `packages/auth/packages/core` | P0 | 15h |
| `@ldesign/auth-vue` | `packages/auth/packages/vue` | P0 | 10h |
| `@ldesign/permission-core` | `packages/permission/packages/core` | P0 | 10h |
| `@ldesign/permission-vue` | `packages/permission/packages/vue` | P0 | 8h |
| `@ldesign/layout-core` | `packages/layout/packages/core` | P0 | 12h |
| `@ldesign/layout-vue` | `packages/layout/packages/vue` | P0 | 15h |
| `@ldesign/menu-core` | `packages/menu/packages/core` | P1 | 10h |
| `@ldesign/menu-vue` | `packages/menu/packages/vue` | P1 | 12h |
| `@ldesign/tabs-core` | `packages/tabs/packages/core` | P1 | 10h |
| `@ldesign/tabs-vue` | `packages/tabs/packages/vue` | P1 | 12h |
| `@ldesign/command-core` | `packages/command/packages/core` | P2 | 8h |
| `@ldesign/command-vue` | `packages/command/packages/vue` | P2 | 10h |

**总计**: 约 120-130 小时（15-16 个工作日）

---

## 🔗 参考资源

**现有实现（可参考）**:
- `libraries/webcomponent/src/components/menu` - 菜单 Web Component
- `libraries/webcomponent/src/components/tabs` - 标签页 Web Component
- `packages/router/packages/core/src/features/permissions.ts` - 权限基础
- `libraries/editor/packages/core/src/enterprise/auth` - 认证参考

**设计参考**:
- Ant Design Pro Layout
- Naive UI Admin
- Vue Vben Admin

